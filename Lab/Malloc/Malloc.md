本人于2025.9.29放弃此Lab :(

这个lab陆陆续续写了一周左右的时间，用时大概10~20h，由于一个bug无论如何也无法修复，遂放弃。

这道题书上给出了最简易的隐式空闲链表的做法，得分大概在62左右。我使用的是分离适配+第一次适配的策略，如果通关大约有84分。

使用分离适配需要将所有大小块的指针统一放置在堆顶，并在`mm_init`函数中完成初始化，堆的结构为：大小块指针，序言块，内存分配区，结尾块。我的代码实现并没有将大小块指针放在堆顶，而是选择构造了一个全局指针数组（WriteUp里应该不允许这样，可能是因为这个导致了一些错误）。

具体的代码实现放在Github，倒是通过这次Lab相当程序上熟练了gdb的使用，也对segmentation fault有了一定的处理手段。coding一时爽，debug火葬场了属于是。

这个Lab网上有非常多90+的实现，但思路大体没变，本人也不想再去扣那些细节了，学校这学期的ICS可能还要再写一次这个Lab，希望到时候可以debug顺利一点🙏